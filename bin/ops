#!/usr/bin/env ruby

require 'bundler/setup'
require 'ops'

cli = Ops::Apollo
cli.login('apollo', 'admin')

# puts cli.conn
# p cli.apps
# puts cli.app? "test1"
# puts cli.app? "SampleApp"
# puts cli.app_auth("admin", "123456")

puts cli.app!('t4')
puts cli.app_auth('apollo', 'DEV', 't4', 'application')
puts cli.app_delete!('test1')
puts cli.app_delete!('test2')

# logout
cli.logout
